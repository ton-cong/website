<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.NotificationRepository">

    <resultMap id="NotificationResultMap" type="com.example.demo.entity.Notification">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="type" column="type"/>
        <result property="content" column="content"/>
        <result property="isRead" column="is_read"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.example.demo.entity.Notification">
        INSERT INTO Notification (user_id, type, content, is_read, created_at)
        VALUES (#{userId}, #{type}, #{content}, #{isRead}, #{createdAt})
    </insert>

    <select id="findByUserId" resultMap="NotificationResultMap">
        SELECT * FROM Notification
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT 50
    </select>

    <update id="markAsRead">
        UPDATE Notification
        SET is_read = 1
        WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE Notification
        SET is_read = 1
        WHERE user_id = #{userId}
    </update>

    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM Notification
        WHERE user_id = #{userId} AND is_read = 0
    </select>

</mapper>
